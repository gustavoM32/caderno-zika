alias gdb="gdb --silent"
alias memtime="/usr/bin/time -f '%C %Us %MKB"

# make alt-tab change only to windows in current workspace
gsettings set org.gnome.shell.app-switcher current-workspace-only true
